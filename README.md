# marketplace-order-updates

Deploy repo for the **order-updates** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs order-updates --push`.

Coolify: base_directory `/`, dockerfile `/apps/order-updates/Dockerfile`.
